=====================
various documentation
=====================

text decoration
================

| this **word** is strong.
| this *text* is italic

table list
================

table list
::::::::::::::
====== ======== ======= ================
ID      name     age     status
====== ======== ======= ================
1000   jon       55     upper middle
2000   ken       12     kids
3000   Lisa      14     teen
4000   Teru      60     old
5000   Carly     35     middle
====== ======== ======= ================

section 2
============

sub section
::::::::::::::
itemization list

- 一番 : this means number one
- 二番 : this means number two
- 三番 : this means number three

sub sub section
-----------------

enum list

#. foo foo
#. baa foo
#. hoge foo
#. hage foo

sub sub section
-----------------

python code

::

    import numpy as np
    import pandas as pd
    import pickle
    from gensim.models import word2vec
    import MeCab

    tagger = MeCab.Tagger('-Owakati -d /usr/local/lib/mecab/dic/mecab-ipadic-neologd')#タグはMeCab.Tagger（neologd辞書）を使用

    tagger.parse('')
    def tokenize_ja(text, lower):
        node = tagger.parseToNode(str(text))
        while node:
            if lower and node.feature.split(',')[0] in ["名詞","形容詞"]:#分かち書きで取得する品詞を指定
                yield node.surface.lower()
            node = node.next
    def tokenize(content, token_min_len, token_max_len, lower):
        return [
            str(token) for token in tokenize_ja(content, lower)
            if token_min_len <= len(token) <= token_max_len and not token.startswith('_')
        ]

section 3
============
#. you're so fool
#. oh sit
#. Dark side of the world

definision list

一
  一 is japanese kanji. this means one.
二
  二 is too. this means two.


